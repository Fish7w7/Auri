import { useEffect, useState } from 'react'
import type { BackupPreview, BackupState, CoverCacheUsage, ImportPreview, LibrarySort, LibraryView, SystemStatus } from '@shared/contracts'
import { useAppContext } from '../app/app-context'
import { LoadingState } from '../components/ui/States'
import { Button } from '../components/ui/Button'
import { Select } from '../components/ui/Select'
import { useToast } from '../components/ui/Toast'
import { Dialog } from '../components/ui/Dialog'
import { applyLibraryImport } from '../lib/apply-library-import'
import { UpdatesSettings } from '../components/settings/UpdatesSettings'

const sections = ['Geral', 'Aparência', 'Biblioteca', 'Backup', 'Atualizações', 'Avançado', 'Sobre'] as const
type Section = (typeof sections)[number]

const sortLabels: Record<LibrarySort, string> = { last_read_desc: 'Última leitura', last_read_asc: 'Mais tempo sem ler', title_asc: 'Título A–Z', title_desc: 'Título Z–A', created_desc: 'Adicionado recentemente', updated_desc: 'Atualizado recentemente', chapter_desc: 'Capítulo', rating_desc: 'Nota' }

export function SettingsPage() {
  const { settings, updateSettings, refreshData } = useAppContext()
  const [section, setSection] = useState<Section>('Geral')
  const [system, setSystem] = useState<SystemStatus | null>(null)
  const [cache, setCache] = useState<CoverCacheUsage | null>(null)
  const [backup, setBackup] = useState<BackupState | null>(null)
  const [restorePreview, setRestorePreview] = useState<BackupPreview | null>(null)
  const [importPreview, setImportPreview] = useState<ImportPreview | null>(null)
  const [busy, setBusy] = useState(false)
  const { showToast } = useToast()
  const loadCache = () => void window.lumi.covers.usage().then(setCache).catch(() => setCache(null))
  useEffect(() => { void window.lumi.system.getStatus().then(setSystem); loadCache() }, [])
  useEffect(() => { if (section === 'Backup') void window.lumi.backup.state().then(setBackup) }, [section, settings])
  const dataAction = async (action: () => Promise<void>, success: string) => { setBusy(true); try { await action(); setBackup(await window.lumi.backup.state()); showToast({ kind: 'success', message: success }) } catch (error) { showToast({ kind: 'error', message: error instanceof Error ? error.message : 'Não foi possível concluir a operação.' }) } finally { setBusy(false) } }
  return <div className="page settings-page"><header className="page-header"><div><span className="page-kicker">Preferências do aplicativo</span><h1>Configurações</h1></div></header><div className="settings-layout"><nav aria-label="Seções de Configurações">{sections.map((item) => <button key={item} className={section === item ? 'is-active' : ''} onClick={() => setSection(item)}>{item}</button>)}</nav><section className="settings-panel">
    {section === 'Backup' && <div className="data-explanation"><p><strong>Backup</strong> é a cópia completa para restaurar o Lumi exatamente como estava.</p><p><strong>JSON</strong> contém dados portáveis, <strong>CSV</strong> é uma visão para planilhas e <strong>Importar JSON</strong> mescla bibliotecas.</p></div>}
    {section === 'Geral' && <SettingsPlaceholder title="Geral" text="As preferências gerais de janela serão adicionadas quando tiverem comportamento real implementado." />}
    {section === 'Aparência' && <><h2>Aparência</h2><div className="setting-row"><div><strong>Tema</strong><p>O Lumi usa o tema escuro nesta versão.</p></div><span className="setting-value">Escuro</span></div><div className="setting-row"><div><strong>Tamanho dos cards</strong><p>Ajusta a densidade da grade da Biblioteca.</p></div><Select label="Tamanho dos cards" value={settings.cardSize} onChange={(cardSize) => void updateSettings({ cardSize: cardSize as 'small' | 'medium' | 'large' })} options={[{ value: 'small', label: 'Pequeno' }, { value: 'medium', label: 'Médio' }, { value: 'large', label: 'Grande' }]} /></div></>}
    {section === 'Biblioteca' && <><h2>Biblioteca</h2><div className="setting-row"><div><strong>Visualização padrão</strong><p>Modo usado ao abrir a Biblioteca.</p></div><Select label="Visualização padrão" value={settings.libraryView} onChange={(libraryView) => void updateSettings({ libraryView: libraryView as LibraryView })} options={[{ value: 'grid', label: 'Grade' }, { value: 'list', label: 'Lista' }]} /></div><div className="setting-row"><div><strong>Ordenação padrão</strong><p>Filtros temporários não são persistidos.</p></div><Select label="Ordenação padrão" value={settings.librarySort} onChange={(librarySort) => void updateSettings({ librarySort: librarySort as LibrarySort })} options={Object.entries(sortLabels).map(([value, label]) => ({ value, label }))} /></div><div className="setting-row"><div><strong>Cache de capas</strong><p>{cache ? `${cache.files} arquivos · ${formatBytes(cache.bytes)} usados` : 'Calculando uso…'} · limite configurado de {settings.coverCacheMaxMb} MB.</p></div><Button onClick={async () => { const usage = await window.lumi.covers.clearAll(); setCache(usage); window.dispatchEvent(new Event('lumi:cover-cache-changed')); showToast({ kind: 'success', message: 'Cache de capas limpo. Capas personalizadas foram preservadas.' }) }}>Limpar cache</Button></div></>}
    {section === 'Backup' && <><h2>Backup e portabilidade</h2><div className="setting-row"><div><strong>Backup automático</strong><p>Protege banco, preferências e arquivos permanentes sem incluir caches.</p></div><label className="setting-toggle"><input type="checkbox" checked={settings.backupAutomatic} onChange={(event) => void updateSettings({ backupAutomatic: event.target.checked })} /> Ativado</label></div><div className="setting-row"><div><strong>Frequência e retenção</strong><p>A retenção se aplica somente aos backups automáticos.</p></div><div className="setting-inline"><Select label="Frequência" value={settings.backupFrequency} onChange={(backupFrequency) => void updateSettings({ backupFrequency: backupFrequency as 'daily' | 'weekly' })} options={[{ value: 'daily', label: 'Diário' }, { value: 'weekly', label: 'Semanal' }]} /><Select label="Retenção" value={String(settings.backupRetention)} onChange={(backupRetention) => void updateSettings({ backupRetention: Number(backupRetention) })} options={[5, 10, 20, 30].map((value) => ({ value: String(value), label: `${value} backups` }))} /></div></div><div className="setting-row"><div><strong>Pasta dos backups</strong><p className={!backup?.directoryAvailable ? 'setting-warning' : ''}>{backup?.directory ?? 'Carregando…'}{backup && !backup.directoryAvailable ? ' · pasta indisponível; o Lumi usará a pasta padrão.' : ''}</p></div><div className="setting-inline"><Button disabled={busy} onClick={() => void window.lumi.backup.openFolder()}>Abrir pasta</Button><Button disabled={busy} onClick={async () => { const state = await window.lumi.backup.chooseDirectory(); if (state) setBackup(state) }}>Alterar</Button></div></div><div className="data-actions"><Button variant="primary" disabled={busy} onClick={() => void dataAction(async () => { await window.lumi.backup.create() }, 'Backup criado com sucesso.')}>Fazer backup agora</Button><Button disabled={busy} onClick={async () => { const preview = await window.lumi.backup.chooseRestore(); if (preview) setRestorePreview(preview) }}>Restaurar backup</Button><Button disabled={busy} onClick={() => void dataAction(async () => { const result = await window.lumi.transfer.exportJson(); if (!result) throw new Error('Exportação cancelada.') }, 'Biblioteca exportada em JSON.')}>Exportar JSON</Button><Button disabled={busy} onClick={() => void dataAction(async () => { const result = await window.lumi.transfer.exportCsv(); if (!result) throw new Error('Exportação cancelada.') }, 'Resumo exportado em CSV.')}>Exportar CSV</Button><Button disabled={busy} onClick={async () => { try { const preview = await window.lumi.transfer.chooseImport(); if (preview) setImportPreview(preview) } catch (error) { showToast({ kind: 'error', message: error instanceof Error ? error.message : 'Arquivo de importação inválido.' }) } }}>Importar JSON</Button></div><div className="backup-list"><h3>Backups disponíveis</h3>{backup?.backups.length ? backup.backups.map((item) => <article key={item.path}><div><strong>{backupTypeLabel(item.type)}</strong><p>{new Date(item.createdAt).toLocaleString('pt-BR')} · {formatBytes(item.size)}</p></div><Button variant="ghost" disabled={busy} onClick={() => { if (window.confirm(`Excluir ${item.fileName}?`)) void dataAction(async () => window.lumi.backup.delete({ path: item.path }), 'Backup excluído.') }}>Excluir</Button></article>) : <p className="muted-copy">Nenhum backup criado nesta pasta.</p>}</div></>}
    {section === 'Atualizações' && <UpdatesSettings />}
    {section === 'Avançado' && <><h2>Diagnóstico</h2>{system ? <div className="diagnostic-card"><dl><div><dt>Banco local</dt><dd>Disponível</dd></div><div><dt>Schema</dt><dd>{system.database.schemaVersion}</dd></div><div><dt>SQLite</dt><dd>{system.database.sqliteVersion}</dd></div><div><dt>Diretório de dados</dt><dd title={system.paths.root}>{system.paths.root}</dd></div></dl></div> : <LoadingState />}</>}
    {section === 'Sobre' && <><h2>Sobre</h2><div className="about-brand"><div className="brand-mark brand-mark--large">L</div><div><strong>Lumi</strong><p>Sua biblioteca pessoal, preservada localmente.</p></div></div>{system && <dl className="about-details"><div><dt>Versão do app</dt><dd>{system.appVersion}</dd></div><div><dt>Database schema</dt><dd>{system.database.schemaVersion}</dd></div></dl>}</>}
  </section></div><Dialog open={!!restorePreview} title="Confirmar restauração" description={restorePreview ? `Backup de ${new Date(restorePreview.createdAt).toLocaleString('pt-BR')}, com ${restorePreview.workCount} obras. O Lumi criará um backup de segurança e reiniciará após restaurar.` : ''} onClose={() => setRestorePreview(null)} footer={<><Button onClick={() => setRestorePreview(null)}>Cancelar</Button><Button variant="danger" disabled={busy} onClick={() => void dataAction(async () => { await window.lumi.backup.restore({ path: restorePreview!.path }); setRestorePreview(null) }, 'Backup restaurado. O Lumi será reiniciado.')}>Restaurar e reiniciar</Button></>} />
  <Dialog open={!!importPreview} title="Revisar importação" description={importPreview ? `${importPreview.total} obras: ${importPreview.newWorks} novas, ${importPreview.exactMatches} correspondências exatas, ${importPreview.probableMatches} prováveis, ${importPreview.trashMatches} na Lixeira e ${importPreview.conflicts} conflitos.` : ''} onClose={() => setImportPreview(null)}><div className="import-preview"><p>Correspondências prováveis ficam separadas para evitar duplicação silenciosa. Escolha como tratar conflitos exatos:</p><label><input type="checkbox" id="restore-import-trash" /> Restaurar correspondências encontradas na Lixeira</label><div className="data-actions"><Button disabled={busy} onClick={() => void applyImportChoice('keep_current')}>Manter dados atuais</Button><Button variant="primary" disabled={busy} onClick={() => void applyImportChoice('use_imported')}>Usar dados importados</Button></div></div></Dialog></div>

  async function applyImportChoice(strategy: 'keep_current' | 'use_imported') { if (!importPreview) return; const restoreTrash = (document.getElementById('restore-import-trash') as HTMLInputElement | null)?.checked ?? false; await dataAction(async () => { const result = await applyLibraryImport(window.lumi.transfer.applyImport, { path: importPreview.path, strategy, restoreTrash }, refreshData); setImportPreview(null); if (result.skipped) showToast({ kind: 'info', message: `${result.skipped} correspondências prováveis ou itens da Lixeira não foram alterados.` }) }, 'Importação concluída.') }
}

function SettingsPlaceholder({ title, text }: { title: string; text: string }) { return <><h2>{title}</h2><div className="settings-placeholder"><span>Em breve</span><p>{text}</p></div></> }
function formatBytes(bytes: number): string { return bytes < 1024 * 1024 ? `${Math.round(bytes / 1024)} KB` : `${(bytes / 1024 / 1024).toFixed(1)} MB` }
function backupTypeLabel(type: BackupState['backups'][number]['type']): string { return ({ manual: 'Backup manual', auto: 'Backup automático', before_restore: 'Antes da restauração', before_import: 'Antes da importação', before_migration: 'Antes da migração' })[type] }
